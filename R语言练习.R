# 查看函数的帮助命令
# help("mode")

# 创建一个向量
v1 = c(1:100)

# 查看对象的数据类型
mode(v1)

# 创建一个矩阵
A <- matrix(c(1:12),nrow = 3)
A

# 创建矩阵
B <- matrix(c(13:2),nrow = 4)
B

# 矩阵乘法
C <- A %*% B
C

# 创建两个向量
a <- c(1,2,3,4,5)
b <- 6:10
a
b
# 将两个向量横向绑定成矩阵
rbind(a, b)
# 将两个向量纵向绑定成矩阵
cbind(a,b)

# 将向量转换成矩阵
c <- matrix(1:16,4,4)

# 删除矩阵中1-10这10个元素
c[-(1:10)]

# 生成队列
s <- seq(1,10)
s

# 区间内步长为2 的队列
s1 <- seq(1,10,by=2)

# 生成一个队列,在区间范围内共10个元素
s2 <- seq(1,100,length=10)

# 生成26个字母
e = letters[seq(1,26)]














